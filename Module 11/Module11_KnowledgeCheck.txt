A pointer is indicated with * and holds the address of a variable (This can be used to pass large datasets by passing its address using a pointer)

"&" gives the address of a variable/the location a value is stored